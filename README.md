# test_version_express
First repository
To do list:
You need Node.js installed
run at cmd: "npm install express --save" (node_modules folder, package.json and package-lock.json will be created)
run at cmd: "node mar_ExpressServer.js" (the server will start and listens at port 3001)
in browser: "http://localhost:3001"
Note: at the moment, there is no Java and/or JavaFx runtime provided. I work with jdk-15.0.2 and javafx-sdk-15.01
Only the buttons "call MarWeb", "call mp4" and "do_any_thing" are active, i. e. should response.

NEW on 17-06-2026:
- Added directories node_modules and publicx
- Added files marfin.bat, stage1.bat, package.json and package-lock.json
- Uploaded modified files forExpressServer_mp4.html, index-buttons_espress.html and mar_ExpressServer.js
